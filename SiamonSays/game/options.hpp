#pragma once

enum MainMenuOptions
{
    noAct,singleplayer=1, multiplayer, Exit
};

enum Difficulties
{
    noActive,easy=1, medium, hard
};

enum GamesButton
{
    stay=0, pause =5 ,turquoise=1, purple,green,blue
};

enum PauseMenu
{
    none, exitGame=1,Continue, mainMenu
};